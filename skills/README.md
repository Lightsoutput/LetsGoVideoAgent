# Project Skills

`Skill Studio` 生成的领域 Skill 在人工发布后写入 `generated/<slug>/v<version>/SKILL.md`。
草案只保存在 Repository 中，不会进入运行时目录；发布版本可在前端绑定视频、回滚和查看 Trace。

用户生成内容默认不提交 Git。需要团队共享时，应先完成评测与隐私检查，再单独建立受控 Skill 包。
